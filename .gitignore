node_modules/
dist/
.env
*.log
*.tsbuildinfo
.DS_Store

# Editor / tooling
.grok/
.vscode/
.idea/
.claude.json
.mcp.json

# Python virtualenv
bin/
lib/
lib64
pyvenv.cfg
share/

# Reproducible IEEE OUI databases (fetched by scripts/download-oui.sh or the Docker build)
/data/*.csv

# Stale compiled output from the types-only shared package (source is types.ts)
shared/types.js
shared/types.d.ts

# Independent git repo for the public GitHub Pages demo site — nested here for
# tooling convenience only, tracked entirely by its own repo, not this one.
/librenms-dash.github.io/
